// pages/index.js

import React from 'react';

const Home = () => {
return (
	<div
	style={{
		display: 'flex',
		justifyContent: 'centre',
		alignItems: 'centre',
		height: '100vh'
	}}
	>
	<h1>Layout</h1>
    <p>similar to cashapp layout</p>
    <p>add snapshots of what user can see</p>
    <p>locked tabs only available to see after sign up/in </p>
	</div>
);
};

export default Home;
